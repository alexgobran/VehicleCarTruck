using System;
using System.Collections.Generic;
using VehicleCarTruckLib;

namespace VehicleCarandTruck {
    class Program {

        //instead of creating a type of vehicle ( car, truck) in the vehicle class. Creating a new Class called Car, and another Class called Truck.
        //opposed to the alternative of have all in the vehicle class by using  string Type;

        static void Main(string[] args) {
            (new Program()).Run();

            
        }

        void Run() {

            var vehicles = new List<Vehicles>();
            vehicles.Add(new)

           

            Console.WriteLine("Hello World!");
        }
    }
}
